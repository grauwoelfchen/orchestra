#!/usr/bin/env ruby
# encoding: utf-8

require "rinda/tuplespace"
require "orchestra/stream"

$stdout.sync = true

DRb.start_service
staff = DRbObject.new_with_uri("druby://localhost:12345")
staff_p = Rinda::TupleSpaceProxy.new(staff)
Stream.new(staff_p).main_loop
